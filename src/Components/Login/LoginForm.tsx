import styled from "styled-components";

const LoginTitle = styled.h1`
  font-size: 40px;
`;

const LoginForm: React.FC = () => {
  return <LoginTitle>Login</LoginTitle>;
};

export default LoginForm;
